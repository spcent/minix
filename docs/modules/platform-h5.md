# H5 Platform Module

Path: `packages/platform-h5`

Use this package for:

- browser request, storage, router, and auth adapters
- browser-oriented UI integration
- H5-only adapter behavior required to satisfy shared ports

This package exists to prove that shared abstractions are not WeChat-specific. It should stay thin and focused on adapter concerns.
